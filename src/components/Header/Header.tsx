import React from "react";
import { StyledHeader } from "./styles";
import { InputForm } from "components";

export const Header = () => {
  return (
    <StyledHeader>
      <InputForm />
    </StyledHeader>
  );
};
